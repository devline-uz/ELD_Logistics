## 1.0.0

- Go `backend/internal/hos` paketining sof Dart porti (M3 bosqichi).
- 35/35 golden vektor (`hos-test-vectors.json`, SHA-256 qulfi bilan).
- `computeCounters`, `dayTotals`, `violations`, `recap`, sleeper split, 34h
  restart, PC/YM, Home Terminal TZ kun chegarasi (DST bilan).
