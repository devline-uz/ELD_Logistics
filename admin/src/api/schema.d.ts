/**
 * @generated
 *
 * GENERATSIYA NATIJASI — QO'LDA TEGILMAYDI.
 * Yangilash: `DOCS_TOKEN=<token> npm run api`
 * (openapi/swagger.json yuklab olinadi → openapi-typescript bu faylni qayta yozadi).
 *
 * HOZIRCHA PLACEHOLDER: `GET /api/docs/swagger.json` 401 UNAUTHORIZED qaytardi
 * ("api docs requires a bearer token"), DOCS_TOKEN hali berilmagan.
 * Token olinishi bilan bu fayl haqiqiy sxema bilan almashtiriladi.
 */

export interface paths {
  [path: string]: unknown;
}

export interface webhooks {
  [name: string]: unknown;
}

export interface components {
  schemas: {
    [name: string]: unknown;
  };
}

export type $defs = Record<string, never>;

export type operations = Record<string, never>;
