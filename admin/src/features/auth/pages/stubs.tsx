import { PageStub } from '@/components/feedback/PageStub';

/**
 * Vaqtinchalik joy egallovchi — haqiqiy login/2FA/parol tiklash ekranlari
 * 0.11–0.15 vazifalarida (`DOCS_TOKEN` blokeri yechilgach) quriladi.
 */
export const LoginPage = () => <PageStub titleKey="pages.login.title" stage={0} />;
