// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: unidentified_events.sql

package db

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const annotateUnidentifiedEvent = `-- name: AnnotateUnidentifiedEvent :one
UPDATE unidentified_events
SET status = 'annotated', annotation = $3, resolved_by = $4, resolved_at = now()
WHERE company_id = $1 AND id = $2 AND status = 'pending'
RETURNING id, company_id, unit_id, eld_device_id, start_at, end_at, distance_m, track_key, status, assigned_driver_id, annotation, resolved_by, resolved_at, created_at, updated_at, edit_request_id
`

type AnnotateUnidentifiedEventParams struct {
	CompanyID  uuid.UUID
	ID         uuid.UUID
	Annotation *string
	ResolvedBy pgtype.UUID
}

func (q *Queries) AnnotateUnidentifiedEvent(ctx context.Context, arg AnnotateUnidentifiedEventParams) (UnidentifiedEvent, error) {
	row := q.db.QueryRow(ctx, annotateUnidentifiedEvent,
		arg.CompanyID,
		arg.ID,
		arg.Annotation,
		arg.ResolvedBy,
	)
	var i UnidentifiedEvent
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.UnitID,
		&i.EldDeviceID,
		&i.StartAt,
		&i.EndAt,
		&i.DistanceM,
		&i.TrackKey,
		&i.Status,
		&i.AssignedDriverID,
		&i.Annotation,
		&i.ResolvedBy,
		&i.ResolvedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.EditRequestID,
	)
	return i, err
}

const assignUnidentifiedEvent = `-- name: AssignUnidentifiedEvent :one
UPDATE unidentified_events
SET status = 'assigned', assigned_driver_id = $3, resolved_by = $4, resolved_at = now()
WHERE company_id = $1 AND id = $2 AND status = 'pending'
RETURNING id, company_id, unit_id, eld_device_id, start_at, end_at, distance_m, track_key, status, assigned_driver_id, annotation, resolved_by, resolved_at, created_at, updated_at, edit_request_id
`

type AssignUnidentifiedEventParams struct {
	CompanyID        uuid.UUID
	ID               uuid.UUID
	AssignedDriverID pgtype.UUID
	ResolvedBy       pgtype.UUID
}

func (q *Queries) AssignUnidentifiedEvent(ctx context.Context, arg AssignUnidentifiedEventParams) (UnidentifiedEvent, error) {
	row := q.db.QueryRow(ctx, assignUnidentifiedEvent,
		arg.CompanyID,
		arg.ID,
		arg.AssignedDriverID,
		arg.ResolvedBy,
	)
	var i UnidentifiedEvent
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.UnitID,
		&i.EldDeviceID,
		&i.StartAt,
		&i.EndAt,
		&i.DistanceM,
		&i.TrackKey,
		&i.Status,
		&i.AssignedDriverID,
		&i.Annotation,
		&i.ResolvedBy,
		&i.ResolvedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.EditRequestID,
	)
	return i, err
}

const countPendingUnidentifiedEvents = `-- name: CountPendingUnidentifiedEvents :one
SELECT count(*) FROM unidentified_events WHERE company_id = $1 AND status = 'pending'
`

func (q *Queries) CountPendingUnidentifiedEvents(ctx context.Context, companyID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countPendingUnidentifiedEvents, companyID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countUnidentifiedEvents = `-- name: CountUnidentifiedEvents :one
SELECT count(*) FROM unidentified_events e
WHERE e.company_id = $1
  AND ($2::text IS NULL OR e.status = $2::text)
  AND ($3::uuid IS NULL OR e.unit_id = $3::uuid)
  AND ($4::timestamptz IS NULL OR e.start_at >= $4::timestamptz)
  AND ($5::timestamptz IS NULL OR e.start_at < $5::timestamptz)
`

type CountUnidentifiedEventsParams struct {
	CompanyID uuid.UUID
	Column2   string
	Column3   uuid.UUID
	Column4   time.Time
	Column5   time.Time
}

func (q *Queries) CountUnidentifiedEvents(ctx context.Context, arg CountUnidentifiedEventsParams) (int64, error) {
	row := q.db.QueryRow(ctx, countUnidentifiedEvents,
		arg.CompanyID,
		arg.Column2,
		arg.Column3,
		arg.Column4,
		arg.Column5,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createUnidentifiedEvent = `-- name: CreateUnidentifiedEvent :one
INSERT INTO unidentified_events (company_id, unit_id, eld_device_id, start_at, end_at, distance_m, track_key)
VALUES ($1,$2,$3,$4,$5,$6,$7) RETURNING id, company_id, unit_id, eld_device_id, start_at, end_at, distance_m, track_key, status, assigned_driver_id, annotation, resolved_by, resolved_at, created_at, updated_at, edit_request_id
`

type CreateUnidentifiedEventParams struct {
	CompanyID   uuid.UUID
	UnitID      uuid.UUID
	EldDeviceID pgtype.UUID
	StartAt     time.Time
	EndAt       pgtype.Timestamptz
	DistanceM   int64
	TrackKey    *string
}

func (q *Queries) CreateUnidentifiedEvent(ctx context.Context, arg CreateUnidentifiedEventParams) (UnidentifiedEvent, error) {
	row := q.db.QueryRow(ctx, createUnidentifiedEvent,
		arg.CompanyID,
		arg.UnitID,
		arg.EldDeviceID,
		arg.StartAt,
		arg.EndAt,
		arg.DistanceM,
		arg.TrackKey,
	)
	var i UnidentifiedEvent
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.UnitID,
		&i.EldDeviceID,
		&i.StartAt,
		&i.EndAt,
		&i.DistanceM,
		&i.TrackKey,
		&i.Status,
		&i.AssignedDriverID,
		&i.Annotation,
		&i.ResolvedBy,
		&i.ResolvedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.EditRequestID,
	)
	return i, err
}

const getUnidentifiedEvent = `-- name: GetUnidentifiedEvent :one
SELECT id, company_id, unit_id, eld_device_id, start_at, end_at, distance_m, track_key, status, assigned_driver_id, annotation, resolved_by, resolved_at, created_at, updated_at, edit_request_id FROM unidentified_events WHERE company_id = $1 AND id = $2
`

type GetUnidentifiedEventParams struct {
	CompanyID uuid.UUID
	ID        uuid.UUID
}

func (q *Queries) GetUnidentifiedEvent(ctx context.Context, arg GetUnidentifiedEventParams) (UnidentifiedEvent, error) {
	row := q.db.QueryRow(ctx, getUnidentifiedEvent, arg.CompanyID, arg.ID)
	var i UnidentifiedEvent
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.UnitID,
		&i.EldDeviceID,
		&i.StartAt,
		&i.EndAt,
		&i.DistanceM,
		&i.TrackKey,
		&i.Status,
		&i.AssignedDriverID,
		&i.Annotation,
		&i.ResolvedBy,
		&i.ResolvedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.EditRequestID,
	)
	return i, err
}

const listUnidentifiedEvents = `-- name: ListUnidentifiedEvents :many
SELECT e.id, e.company_id, e.unit_id, e.eld_device_id, e.start_at, e.end_at, e.distance_m, e.track_key, e.status, e.assigned_driver_id, e.annotation, e.resolved_by, e.resolved_at, e.created_at, e.updated_at, e.edit_request_id, u.unit_number
FROM unidentified_events e
JOIN units u ON u.id = e.unit_id
WHERE e.company_id = $1
  AND ($2::text IS NULL OR e.status = $2::text)
  AND ($3::uuid IS NULL OR e.unit_id = $3::uuid)
  AND ($4::timestamptz IS NULL OR e.start_at >= $4::timestamptz)
  AND ($5::timestamptz IS NULL OR e.start_at < $5::timestamptz)
ORDER BY e.start_at DESC
LIMIT $6 OFFSET $7
`

type ListUnidentifiedEventsParams struct {
	CompanyID uuid.UUID
	Column2   string
	Column3   uuid.UUID
	Column4   time.Time
	Column5   time.Time
	Limit     int32
	Offset    int32
}

type ListUnidentifiedEventsRow struct {
	ID               uuid.UUID
	CompanyID        uuid.UUID
	UnitID           uuid.UUID
	EldDeviceID      pgtype.UUID
	StartAt          time.Time
	EndAt            pgtype.Timestamptz
	DistanceM        int64
	TrackKey         *string
	Status           string
	AssignedDriverID pgtype.UUID
	Annotation       *string
	ResolvedBy       pgtype.UUID
	ResolvedAt       pgtype.Timestamptz
	CreatedAt        time.Time
	UpdatedAt        time.Time
	EditRequestID    pgtype.UUID
	UnitNumber       string
}

func (q *Queries) ListUnidentifiedEvents(ctx context.Context, arg ListUnidentifiedEventsParams) ([]ListUnidentifiedEventsRow, error) {
	rows, err := q.db.Query(ctx, listUnidentifiedEvents,
		arg.CompanyID,
		arg.Column2,
		arg.Column3,
		arg.Column4,
		arg.Column5,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListUnidentifiedEventsRow{}
	for rows.Next() {
		var i ListUnidentifiedEventsRow
		if err := rows.Scan(
			&i.ID,
			&i.CompanyID,
			&i.UnitID,
			&i.EldDeviceID,
			&i.StartAt,
			&i.EndAt,
			&i.DistanceM,
			&i.TrackKey,
			&i.Status,
			&i.AssignedDriverID,
			&i.Annotation,
			&i.ResolvedBy,
			&i.ResolvedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.EditRequestID,
			&i.UnitNumber,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
